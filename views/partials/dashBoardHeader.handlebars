<nav class="navbar nav-class">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">WebSiteName</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">

      <ul class="nav navbar-nav navbar-right" style="display: flex;">
        <img src="/images/search.svg" class="Search">
        <li><a href="/user/{{user.id}}/profile"><span class="glyphicon glyphicon-user"></span> My Profile</a></li>
        <li class="logout-class" onclick="logUserOut()"><span class="glyphicon glyphicon-log-in"></span> Log Out</li>

      </ul>
    </div>
  </div>
</nav>

<script>
    function logUserOut() {
      console.log("Here now");
        var url = "/user/logout";
        var accessToken = "{{ accessToken }}";
        var key = "JWT " + accessToken;
        $.ajax({
            url: url,
            method: "GET",
            headers: {authorization: key},
            success: function(message) {
                console.log("Message:: " + message);
                localStorage.removeItem("accessToken");
                window.location = "http://localhost:1337/user/signup";
            },
            error: function(error) {
                console.dir("Message2:: " + error);
            }
        });
    }
</script>
